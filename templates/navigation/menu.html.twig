{# Menu component #}
{% set directionClass = direction == 'vertical' ? 'flex-column' : '' %}
{% set alignClass = align == 'center' ? 'justify-content-center' : (align == 'end' ? 'd-flex justify-content-end' : 'me-auto') %}

<ul class="navbar-nav mb-2 mb-lg-0 {{ directionClass }} {{ alignClass }} {{ attributes.render('class') }}" {{ attributes.defaults({}) }}>
    {% block content %}
    {% endblock %}
</ul>
